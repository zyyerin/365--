// minimal placeholder day — just the caption (faithful port of the .pde)

function setup() {
  createCanvas(600, 600);
  noLoop();
}

function draw() {
  background(255);
  fill(0);
  noStroke();
  textSize(16);
  text('312--', 24, 36);
}
